# ┌──────────────────────────────────────────────────────────────┐
# │                 Django + DRF Recommended .gitignore          │
# └──────────────────────────────────────────────────────────────┘

# ┌── Python / Django basics ────────────────────────────────────┐
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
*.pyd
.Python
*.log
*.pot
db.sqlite3
db.sqlite3-journal
media/
staticfiles/           # ← usually collected on deployment
local_settings.py      # ← sensitive local config (keep out!)
.env                   # ← secrets, API keys, passwords
.env.*
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# ┌── Testing / Coverage / Cache ────────────────────────────────┐
htmlcov/
.coverage
.coverage.*
.cache
.pytest_cache/
tox/
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.mypy_cache/
.dmypy.json
dmypy.json

# ┌── Development tools & editors ───────────────────────────────┐
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/

# PyCharm / IntelliJ
.idea/
*.iws
out/

# Sublime Text
*.tmlanguage.cache
*.tmPreferences.cache
*.stTheme.cache
*.sublime-workspace
*.sublime-project

# Vim / Neovim
*.swp
*.swo
*.swn
*~

# macOS junk
.DS_Store
.AppleDouble
.LSOverride

# Windows junk
Thumbs.db
ehthumbs.db
Desktop.ini

# ┌── Dependency / build folders ────────────────────────────────┐
pip-log.txt
pip-delete-this-directory.txt
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
*.whl
*.manifest
*.spec
pip-wheel-metadata/

# ┌── Celery / background tasks (if you use them) ───────────────┐
celerybeat-schedule.*
celerybeat.pid

# ┌── Optional: uncomment if you DON'T commit collected static ──┐
# static/

# ┌── Optional: uncomment if you want to ignore ALL migrations   │
# (not recommended for team/projects – usually you WANT them!)  │
# **/migrations/*
# !**/migrations/__init__.py

# ┌── Custom / personal ignores ─────────────────────────────────┐
# Add your own patterns below (notes, temp files, etc)
notes.txt
scratch/
playground/